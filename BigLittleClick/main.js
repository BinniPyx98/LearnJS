const todoLists=[
    {id:"sad122d-sad1d", title:"What to learn", filter:'all', tasks:[{id:"sdads-123cxz", title:"React", isDone:false}]}
]

const little=document.getElementById('little')
const big=document.getElementById('big')

const onClickHandler=()=>{
alert("f")
}

little.addEventListener('click',onClickHandler)
big.addEventListener('click',onClickHandler)